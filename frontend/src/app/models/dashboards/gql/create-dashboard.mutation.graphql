mutation CreateDashboard($input: CreateDashboardInput!) {
  createDashboard(input: $input) {
    id
    name
    widgets {
      id
      widgetType
      title
      subtitle
      config
      cols
      rows
      x
      y
    }
  }
}
