query GetDashboard($id: Int!) {
  dashboard(id: $id) {
    id
    name
    widgets {
      id
      widgetType
      title
      subtitle
      config
      cols
      rows
      x
      y
    }
  }
}
